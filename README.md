# 🥗 SmartDiet-Agent (双脑驱动智能营养配餐系统)

SmartDiet-Agent 是一个面向「个性化饮食建议」的演示型系统：
- 前端使用 Streamlit 提供对话式交互与可视化看板
- 后端基于 Django + SQLite 管理食谱与饮食计划数据
- 同时融合 **LLM（DeepSeek）** 与 **传统机器学习（RandomForest）**，做到“能解释、也能预测”

## 核心亮点

### 1) 双脑架构：LLM + 传统机器学习
- **语言智能（LLM / DeepSeek）**：结合数据库食谱库上下文，生成更自然、更贴近需求的饮食建议与解释
- **预测智能（RandomForest）**：基于用户特征（年龄/身高/体重/活动量）给出策略预测（减脂/维持/增肌），为推荐提供稳定的“底牌”

### 2) 动态科学计算：Mifflin-St Jeor
- 严格采用 **Mifflin-St Jeor** 公式计算 **BMR（基础代谢）**
- 结合活动系数得到 **TDEE（总日消耗）**
- 根据目标（减脂/维持/增肌）动态生成每日目标热量

### 3) 数据可视化：Plotly 宏量营养环形看板
- 基于 Plotly 绘制 **碳水 / 蛋白质 / 脂肪** 的动态环形图
- 将宏量比例与克数建议直观呈现，辅助用户快速理解“怎么吃”

## 快速启动（Windows / PowerShell）

> 下面命令默认在项目根目录执行。

### 1) 安装依赖

```powershell
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
```

### 2) 初始化数据库（Django 迁移）

```powershell
.\.venv\Scripts\python.exe manage.py migrate
```

### 3) 生成传统机器学习模型（可选但推荐）

```powershell
.\.venv\Scripts\python.exe train_ml_model.py
```

### 4) 启动前端（Streamlit）

```powershell
.\.venv\Scripts\python.exe -m streamlit run app.py
```
